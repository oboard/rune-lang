# core/array

Declared placeholder for Rune arrays.

The module exists so array APIs are declared in `/core`, but these functions
will become usable after Rune has array type and literal syntax. The current
stub surface lives in `array.rn`.
