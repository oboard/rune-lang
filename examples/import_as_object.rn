main() => {
  // normal println
  @io.println("Hello, world!")
  // bind @io to a local name
  io := @io
  // bind an imported module object to a local name
  helper := @"./import_helper.rn"
  io.println(helper.greeting("Alice"))
}
