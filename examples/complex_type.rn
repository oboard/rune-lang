fun(flag) => {
  (
    flag ? (x) => {
      k: x.a + 1
    }
      : (y) => {
        k: y.b + 1
      }
  )({
    b: 2,
    z: false,
    a: 1
  }).k
}

main() => {
  res := fun(true) + fun(false)
  @io.println(res)
}
