Return: {
  b: Int
  z: Bool
  a: Int
}

fun(flag) => {
  flag ? (x) => {
    k: x.a + 1,
  } : (y) => {
    k: y.b + 1,
  }
  }(Return { b: 2, z: false, a: 1 }).k
}

main() => {
  @io.println(fun(true) + fun(false))
}
