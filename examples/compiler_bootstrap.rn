@"../selfhost/compiler/compiler.rn"

main() => {
  source := `add(a: Int, b: Int) -> Int => a + b
main() -> Int => add(20, 22)
`
  ts := compileTypeScript(source)
  go := compileGo(source)
  @io.println("typescript")
  @io.println(ts.output)
  @io.println("go")
  @io.println(go.output)
}
