User: {
  id: Int
  name: String
  age: Int

  isAdult() -> Bool => .age >= 18
}

main() => {
  user := User {
    id: 1
    name: "oboard"
    age: 22
  }
  @io.println(user.name)
  @io.println(user.isAdult())
}
